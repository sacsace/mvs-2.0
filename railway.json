{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd server && (npm run init-if-empty || echo 'Init check skipped') && (npm run update-menus || echo 'Menu update skipped') && node dist/index.js",
    "healthcheckPath": "/api/init/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
} 