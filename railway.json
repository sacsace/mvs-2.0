{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd server && (npx ts-node src/scripts/insertBasicDataPostgres.ts || echo 'Init check skipped') && node dist/index.js",
    "healthcheckPath": "/api/init/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
} 